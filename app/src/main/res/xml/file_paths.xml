<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allows access to the external files directory for pictures -->
    <external-files-path
        name="my_images"
        path="Pictures/" />

    <!-- Allows access to the cache directory -->
    <cache-path
        name="my_cache"
        path="." />
</paths>
